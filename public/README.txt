Drop your ASBK logo file here as `logo.png` (the header references `/logo.png`).
Recommended size: 64×64 or larger, square. Transparent PNG looks best on dark and light backgrounds.

For the browser tab icon, also save a copy as `src/app/icon.png`.
